@inherits LayoutComponentBase
@inject IElectronService ElectronService

<ErrorBoundary>
    <ChildContent>
        @Body
    </ChildContent>
    <ErrorContent>
        @{
            Console.WriteLine(context.ToString());
            ElectronService.ShowErrorMessageAsync(context).Wait();
        }
    </ErrorContent>
</ErrorBoundary>

<RadzenComponents @rendermode="InteractiveServer" />
